[[outpus.playfab]]
  ## Your PlayFab Title ID. Required
  # titleId = "AB12"
  
  ## Your PlayFab Developer Secret Key - either this or the telemetryKey must be set, not both
  ## https://learn.microsoft.com/en-us/gaming/playfab/gamemanager/secret-key-management
  # developerSecretKey = "mysecretkey"

  ## Your PlayFab Telemetry Key - either this or the developerSecretKey must be set, not both
  # telemetryKey = "mytelemetrykey"
  
  ## The name of the namespace to use for the PlayFab data
  ## if empty, the namespace will be "custom"
  ## if you give a value, it must be prefixed with "custom." (e.g. "custom.mynamespace")
  # eventNamespace = "mycustomnamespace"

  ## If you want debug messages to be sent to the output, set this to true
  # debug = false